/* Automatically generated qpb constant definitions */
/* Generated by Quick Protocol Buffers - 2 */

#include "RadarCore/Tracks/RearClusterTracks.qpb.h"
#include "Tools/QuickProtobuf/qpb_decode.h"
#include "Tools/QuickProtobuf/qpb_encode.h"
#include "Tools/CSupport/ConversionSupport.h"

#if QPB_PROTO_HEADER_VERSION != 2
#error Regenerate this file with the current version of qpb generator.
#endif

static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_id(qpb_ostream_t *const stream, const uint16_t *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_id(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_closestPoint(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackPosition_Raw *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_closestPoint(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_boxModel(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackModelBox_Raw *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_boxModel(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_movement(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackMovement_Raw *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_movement(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_objectSizeType(qpb_ostream_t *const stream, const RadarCore_Tracks_ObjectSize *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_objectSizeType(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_id(qpb_ostream_t *const stream, const uint16_t *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_id(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_closestPoint(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackPosition *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_closestPoint(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_boxModel(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackModelBox *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_boxModel(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_movement(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackMovement *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_movement(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrack_objectSizeType(qpb_ostream_t *const stream, const RadarCore_Tracks_ObjectSize *const data);
static bool decode_RadarCore_Tracks_RearClusterTrack_objectSizeType(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrackList_Raw_tracks(qpb_ostream_t *const stream, const RadarCore_Tracks_RearClusterTrack_Raw arrayData[], const qpb_size_t arraySize);
static bool decode_RadarCore_Tracks_RearClusterTrackList_Raw_tracks(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_RadarCore_Tracks_RearClusterTrackList_tracks(qpb_ostream_t *const stream, const RadarCore_Tracks_RearClusterTrack arrayData[], const qpb_size_t arraySize);
static bool decode_RadarCore_Tracks_RearClusterTrackList_tracks(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);

#define RadarCore_Tracks_RearClusterTrack_Raw_DECODERS_COUNT 6
const qpb_decoder_entry_t RadarCore_Tracks_RearClusterTrack_Raw_decoders[RadarCore_Tracks_RearClusterTrack_Raw_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_RadarCore_Tracks_RearClusterTrack_Raw_id },
   { &decode_RadarCore_Tracks_RearClusterTrack_Raw_closestPoint },
   { &decode_RadarCore_Tracks_RearClusterTrack_Raw_boxModel },
   { &decode_RadarCore_Tracks_RearClusterTrack_Raw_movement },
   { &decode_RadarCore_Tracks_RearClusterTrack_Raw_objectSizeType }
};

#define RadarCore_Tracks_RearClusterTrack_DECODERS_COUNT 6
const qpb_decoder_entry_t RadarCore_Tracks_RearClusterTrack_decoders[RadarCore_Tracks_RearClusterTrack_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_RadarCore_Tracks_RearClusterTrack_id },
   { &decode_RadarCore_Tracks_RearClusterTrack_closestPoint },
   { &decode_RadarCore_Tracks_RearClusterTrack_boxModel },
   { &decode_RadarCore_Tracks_RearClusterTrack_movement },
   { &decode_RadarCore_Tracks_RearClusterTrack_objectSizeType }
};

#define RadarCore_Tracks_RearClusterTrackList_Raw_DECODERS_COUNT 2
const qpb_decoder_entry_t RadarCore_Tracks_RearClusterTrackList_Raw_decoders[RadarCore_Tracks_RearClusterTrackList_Raw_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_RadarCore_Tracks_RearClusterTrackList_Raw_tracks }
};

#define RadarCore_Tracks_RearClusterTrackList_DECODERS_COUNT 2
const qpb_decoder_entry_t RadarCore_Tracks_RearClusterTrackList_decoders[RadarCore_Tracks_RearClusterTrackList_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_RadarCore_Tracks_RearClusterTrackList_tracks }
};

static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_id(qpb_ostream_t *const stream, const uint16_t *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 8;
      ++stream->buffer;
      qpb_encode_uvarint(stream, *data);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_id(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_uvarint(stream, (void*)&((RadarCore_Tracks_RearClusterTrack_Raw*)data)->id, sizeof(uint16_t));
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_closestPoint(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackPosition_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 18;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_RadarCore_Tracks_ClusterTrackPosition_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_closestPoint(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_RadarCore_Tracks_ClusterTrackPosition_Raw(&substream, &((RadarCore_Tracks_RearClusterTrack_Raw*)data)->closestPoint);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_boxModel(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackModelBox_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 26;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_RadarCore_Tracks_ClusterTrackModelBox_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_boxModel(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_RadarCore_Tracks_ClusterTrackModelBox_Raw(&substream, &((RadarCore_Tracks_RearClusterTrack_Raw*)data)->boxModel);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_movement(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackMovement_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 34;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_RadarCore_Tracks_ClusterTrackMovement_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_movement(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_RadarCore_Tracks_ClusterTrackMovement_Raw(&substream, &((RadarCore_Tracks_RearClusterTrack_Raw*)data)->movement);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_Raw_objectSizeType(qpb_ostream_t *const stream, const RadarCore_Tracks_ObjectSize *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 40;
      ++stream->buffer;
      qpb_encode_varint(stream, *data);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_Raw_objectSizeType(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_varint(stream, (void*)&((RadarCore_Tracks_RearClusterTrack_Raw*)data)->objectSizeType, qpb_membersize(RadarCore_Tracks_RearClusterTrack_Raw, objectSizeType));
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_id(qpb_ostream_t *const stream, const uint16_t *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 8;
      ++stream->buffer;
      qpb_encode_uvarint(stream, *data);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_id(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_uvarint(stream, (void*)&((RadarCore_Tracks_RearClusterTrack*)data)->id, sizeof(uint16_t));
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_closestPoint(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackPosition *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 18;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_RadarCore_Tracks_ClusterTrackPosition(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_closestPoint(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_RadarCore_Tracks_ClusterTrackPosition(&substream, &((RadarCore_Tracks_RearClusterTrack*)data)->closestPoint);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_boxModel(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackModelBox *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 26;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_RadarCore_Tracks_ClusterTrackModelBox(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_boxModel(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_RadarCore_Tracks_ClusterTrackModelBox(&substream, &((RadarCore_Tracks_RearClusterTrack*)data)->boxModel);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_movement(qpb_ostream_t *const stream, const RadarCore_Tracks_ClusterTrackMovement *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 34;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_RadarCore_Tracks_ClusterTrackMovement(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_movement(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_RadarCore_Tracks_ClusterTrackMovement(&substream, &((RadarCore_Tracks_RearClusterTrack*)data)->movement);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_RadarCore_Tracks_RearClusterTrack_objectSizeType(qpb_ostream_t *const stream, const RadarCore_Tracks_ObjectSize *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 40;
      ++stream->buffer;
      qpb_encode_varint(stream, *data);
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrack_objectSizeType(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_varint(stream, (void*)&((RadarCore_Tracks_RearClusterTrack*)data)->objectSizeType, qpb_membersize(RadarCore_Tracks_RearClusterTrack, objectSizeType));
}

static inline void encode_RadarCore_Tracks_RearClusterTrackList_Raw_tracks(qpb_ostream_t *const stream, const RadarCore_Tracks_RearClusterTrack_Raw arrayData[], const qpb_size_t arraySize)
{
   if (arraySize > 0)
   {
      qpb_byte_t* sizePtr;
      qpb_byte_t* dataPtr;
      qpb_size_t size;
      qpb_byte_t dataToClear;
      for (qpb_size_t i = 0; i < arraySize; ++i)
      {
         *stream->buffer = 10;
         ++stream->buffer;
         sizePtr = stream->buffer;
         stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
         dataPtr = stream->buffer;
         encode_RadarCore_Tracks_RearClusterTrack_Raw(stream, &arrayData[i]);
         /* Check size of encoded message and write size at the appropriate offset */
         size = (stream->buffer - dataPtr);
         stream->buffer = sizePtr;
         qpb_encode_varint(stream, (qpb_uint64_t)size);
         dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - sizePtr);
         /* Shift buffer back to end of varint */
         memmove(stream->buffer, dataPtr, size);
         stream->buffer += size;
         memset(stream->buffer, 0, dataToClear);
      }
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrackList_Raw_tracks(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   if ((((RadarCore_Tracks_RearClusterTrackList_Raw*)data)->tracks_count + 1) > 10)
   {
      QPB_RETURN_ERROR(stream, "array overflow");
   }
   {
      qpb_size_t size;
      qpb_istream_t substream;
      bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
      if (!result)
      {
         return false;
      }
      substream = qpb_istream_from_buffer(stream->buffer, size);
      result = decode_RadarCore_Tracks_RearClusterTrack_Raw(&substream, &((RadarCore_Tracks_RearClusterTrackList_Raw*)data)->tracks[((RadarCore_Tracks_RearClusterTrackList_Raw*)data)->tracks_count++]);
      if (!result)
      {
         QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
      }
      stream->buffer += size;
   }
   return true;
}

static inline void encode_RadarCore_Tracks_RearClusterTrackList_tracks(qpb_ostream_t *const stream, const RadarCore_Tracks_RearClusterTrack arrayData[], const qpb_size_t arraySize)
{
   if (arraySize > 0)
   {
      qpb_byte_t* sizePtr;
      qpb_byte_t* dataPtr;
      qpb_size_t size;
      qpb_byte_t dataToClear;
      for (qpb_size_t i = 0; i < arraySize; ++i)
      {
         *stream->buffer = 10;
         ++stream->buffer;
         sizePtr = stream->buffer;
         stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
         dataPtr = stream->buffer;
         encode_RadarCore_Tracks_RearClusterTrack(stream, &arrayData[i]);
         /* Check size of encoded message and write size at the appropriate offset */
         size = (stream->buffer - dataPtr);
         stream->buffer = sizePtr;
         qpb_encode_varint(stream, (qpb_uint64_t)size);
         dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - sizePtr);
         /* Shift buffer back to end of varint */
         memmove(stream->buffer, dataPtr, size);
         stream->buffer += size;
         memset(stream->buffer, 0, dataToClear);
      }
   }
}

static bool decode_RadarCore_Tracks_RearClusterTrackList_tracks(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   if ((((RadarCore_Tracks_RearClusterTrackList*)data)->tracks_count + 1) > 10)
   {
      QPB_RETURN_ERROR(stream, "array overflow");
   }
   {
      qpb_size_t size;
      qpb_istream_t substream;
      bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
      if (!result)
      {
         return false;
      }
      substream = qpb_istream_from_buffer(stream->buffer, size);
      result = decode_RadarCore_Tracks_RearClusterTrack(&substream, &((RadarCore_Tracks_RearClusterTrackList*)data)->tracks[((RadarCore_Tracks_RearClusterTrackList*)data)->tracks_count++]);
      if (!result)
      {
         QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
      }
      stream->buffer += size;
   }
   return true;
}

/* Encoding / decoding functions */
bool encode_RadarCore_Tracks_RearClusterTrack_Raw(qpb_ostream_t *const stream, const RadarCore_Tracks_RearClusterTrack_Raw *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < RadarCore_Tracks_RearClusterTrack_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_RadarCore_Tracks_RearClusterTrack_Raw_id(stream, &data->id);
   encode_RadarCore_Tracks_RearClusterTrack_Raw_closestPoint(stream, &data->closestPoint);
   encode_RadarCore_Tracks_RearClusterTrack_Raw_boxModel(stream, &data->boxModel);
   encode_RadarCore_Tracks_RearClusterTrack_Raw_movement(stream, &data->movement);
   encode_RadarCore_Tracks_RearClusterTrack_Raw_objectSizeType(stream, &data->objectSizeType);
   return true;
}

bool decode_RadarCore_Tracks_RearClusterTrack_Raw(qpb_istream_t *const stream, RadarCore_Tracks_RearClusterTrack_Raw *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < RadarCore_Tracks_RearClusterTrack_Raw_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = RadarCore_Tracks_RearClusterTrack_Raw_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

bool encode_RadarCore_Tracks_RearClusterTrack(qpb_ostream_t *const stream, const RadarCore_Tracks_RearClusterTrack *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < RadarCore_Tracks_RearClusterTrack_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_RadarCore_Tracks_RearClusterTrack_Raw_id(stream, &data->id);
   encode_RadarCore_Tracks_RearClusterTrack_closestPoint(stream, &data->closestPoint);
   encode_RadarCore_Tracks_RearClusterTrack_boxModel(stream, &data->boxModel);
   encode_RadarCore_Tracks_RearClusterTrack_movement(stream, &data->movement);
   encode_RadarCore_Tracks_RearClusterTrack_Raw_objectSizeType(stream, &data->objectSizeType);
   return true;
}

bool decode_RadarCore_Tracks_RearClusterTrack(qpb_istream_t *const stream, RadarCore_Tracks_RearClusterTrack *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < RadarCore_Tracks_RearClusterTrack_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = RadarCore_Tracks_RearClusterTrack_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

bool encode_RadarCore_Tracks_RearClusterTrackList_Raw(qpb_ostream_t *const stream, const RadarCore_Tracks_RearClusterTrackList_Raw *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < RadarCore_Tracks_RearClusterTrackList_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_RadarCore_Tracks_RearClusterTrackList_Raw_tracks(stream, data->tracks, data->tracks_count);
   return true;
}

bool decode_RadarCore_Tracks_RearClusterTrackList_Raw(qpb_istream_t *const stream, RadarCore_Tracks_RearClusterTrackList_Raw *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < RadarCore_Tracks_RearClusterTrackList_Raw_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = RadarCore_Tracks_RearClusterTrackList_Raw_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

bool encode_RadarCore_Tracks_RearClusterTrackList(qpb_ostream_t *const stream, const RadarCore_Tracks_RearClusterTrackList *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < RadarCore_Tracks_RearClusterTrackList_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_RadarCore_Tracks_RearClusterTrackList_tracks(stream, data->tracks, data->tracks_count);
   return true;
}

bool decode_RadarCore_Tracks_RearClusterTrackList(qpb_istream_t *const stream, RadarCore_Tracks_RearClusterTrackList *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < RadarCore_Tracks_RearClusterTrackList_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = RadarCore_Tracks_RearClusterTrackList_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

