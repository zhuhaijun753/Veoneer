/* Automatically generated qpb constant definitions */
/* Generated by Quick Protocol Buffers - 2 */

#include "VisionCore/Road/Freespace.qpb.h"
#include "Tools/QuickProtobuf/qpb_decode.h"
#include "Tools/QuickProtobuf/qpb_encode.h"
#include "Tools/CSupport/ConversionSupport.h"

#if QPB_PROTO_HEADER_VERSION != 2
#error Regenerate this file with the current version of qpb generator.
#endif

static inline void encode_VisionCore_Road_FreespaceSector_Raw_boundaryPosition(qpb_ostream_t *const stream, const VisionCore_Common_Coord2_m_0_01_Raw *const data);
static bool decode_VisionCore_Road_FreespaceSector_Raw_boundaryPosition(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_FreespaceSector_Raw_type(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceBoundaryType *const data);
static bool decode_VisionCore_Road_FreespaceSector_Raw_type(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_FreespaceSector_Raw_confidence(qpb_ostream_t *const stream, const uint8_t *const data);
static bool decode_VisionCore_Road_FreespaceSector_Raw_confidence(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_FreespaceSector_boundaryPosition(qpb_ostream_t *const stream, const VisionCore_Common_Coord2_m_0_01 *const data);
static bool decode_VisionCore_Road_FreespaceSector_boundaryPosition(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_FreespaceSector_type(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceBoundaryType *const data);
static bool decode_VisionCore_Road_FreespaceSector_type(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_FreespaceSector_confidence(qpb_ostream_t *const stream, const uint8_t *const data);
static bool decode_VisionCore_Road_FreespaceSector_confidence(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_Freespace_Raw_sectors(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceSector_Raw arrayData[], const qpb_size_t arraySize);
static bool decode_VisionCore_Road_Freespace_Raw_sectors(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_Freespace_sectors(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceSector arrayData[], const qpb_size_t arraySize);
static bool decode_VisionCore_Road_Freespace_sectors(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);

#define VisionCore_Road_FreespaceSector_Raw_DECODERS_COUNT 4
const qpb_decoder_entry_t VisionCore_Road_FreespaceSector_Raw_decoders[VisionCore_Road_FreespaceSector_Raw_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_VisionCore_Road_FreespaceSector_Raw_boundaryPosition },
   { &decode_VisionCore_Road_FreespaceSector_Raw_type },
   { &decode_VisionCore_Road_FreespaceSector_Raw_confidence }
};

#define VisionCore_Road_FreespaceSector_DECODERS_COUNT 4
const qpb_decoder_entry_t VisionCore_Road_FreespaceSector_decoders[VisionCore_Road_FreespaceSector_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_VisionCore_Road_FreespaceSector_boundaryPosition },
   { &decode_VisionCore_Road_FreespaceSector_type },
   { &decode_VisionCore_Road_FreespaceSector_confidence }
};

#define VisionCore_Road_Freespace_Raw_DECODERS_COUNT 2
const qpb_decoder_entry_t VisionCore_Road_Freespace_Raw_decoders[VisionCore_Road_Freespace_Raw_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_VisionCore_Road_Freespace_Raw_sectors }
};

#define VisionCore_Road_Freespace_DECODERS_COUNT 2
const qpb_decoder_entry_t VisionCore_Road_Freespace_decoders[VisionCore_Road_Freespace_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_VisionCore_Road_Freespace_sectors }
};

static inline void encode_VisionCore_Road_FreespaceSector_Raw_boundaryPosition(qpb_ostream_t *const stream, const VisionCore_Common_Coord2_m_0_01_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 10;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_VisionCore_Common_Coord2_m_0_01_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_VisionCore_Road_FreespaceSector_Raw_boundaryPosition(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_VisionCore_Common_Coord2_m_0_01_Raw(&substream, &((VisionCore_Road_FreespaceSector_Raw*)data)->boundaryPosition);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_VisionCore_Road_FreespaceSector_Raw_type(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceBoundaryType *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 16;
      ++stream->buffer;
      qpb_encode_varint(stream, *data);
   }
}

static bool decode_VisionCore_Road_FreespaceSector_Raw_type(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_varint(stream, (void*)&((VisionCore_Road_FreespaceSector_Raw*)data)->type, qpb_membersize(VisionCore_Road_FreespaceSector_Raw, type));
}

static inline void encode_VisionCore_Road_FreespaceSector_Raw_confidence(qpb_ostream_t *const stream, const uint8_t *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 24;
      ++stream->buffer;
      qpb_encode_uvarint(stream, *data);
   }
}

static bool decode_VisionCore_Road_FreespaceSector_Raw_confidence(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_uvarint(stream, (void*)&((VisionCore_Road_FreespaceSector_Raw*)data)->confidence, sizeof(uint8_t));
}

static inline void encode_VisionCore_Road_FreespaceSector_boundaryPosition(qpb_ostream_t *const stream, const VisionCore_Common_Coord2_m_0_01 *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 10;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_VisionCore_Common_Coord2_m_0_01(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_VisionCore_Road_FreespaceSector_boundaryPosition(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_VisionCore_Common_Coord2_m_0_01(&substream, &((VisionCore_Road_FreespaceSector*)data)->boundaryPosition);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_VisionCore_Road_FreespaceSector_type(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceBoundaryType *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 16;
      ++stream->buffer;
      qpb_encode_varint(stream, *data);
   }
}

static bool decode_VisionCore_Road_FreespaceSector_type(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_varint(stream, (void*)&((VisionCore_Road_FreespaceSector*)data)->type, qpb_membersize(VisionCore_Road_FreespaceSector, type));
}

static inline void encode_VisionCore_Road_FreespaceSector_confidence(qpb_ostream_t *const stream, const uint8_t *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 24;
      ++stream->buffer;
      qpb_encode_uvarint(stream, *data);
   }
}

static bool decode_VisionCore_Road_FreespaceSector_confidence(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_uvarint(stream, (void*)&((VisionCore_Road_FreespaceSector*)data)->confidence, sizeof(uint8_t));
}

static inline void encode_VisionCore_Road_Freespace_Raw_sectors(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceSector_Raw arrayData[], const qpb_size_t arraySize)
{
   if (arraySize > 0)
   {
      qpb_byte_t* sizePtr;
      qpb_byte_t* dataPtr;
      qpb_size_t size;
      qpb_byte_t dataToClear;
      for (qpb_size_t i = 0; i < arraySize; ++i)
      {
         *stream->buffer = 10;
         ++stream->buffer;
         sizePtr = stream->buffer;
         stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
         dataPtr = stream->buffer;
         encode_VisionCore_Road_FreespaceSector_Raw(stream, &arrayData[i]);
         /* Check size of encoded message and write size at the appropriate offset */
         size = (stream->buffer - dataPtr);
         stream->buffer = sizePtr;
         qpb_encode_varint(stream, (qpb_uint64_t)size);
         dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - sizePtr);
         /* Shift buffer back to end of varint */
         memmove(stream->buffer, dataPtr, size);
         stream->buffer += size;
         memset(stream->buffer, 0, dataToClear);
      }
   }
}

static bool decode_VisionCore_Road_Freespace_Raw_sectors(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   if ((((VisionCore_Road_Freespace_Raw*)data)->sectors_count + 1) > 60)
   {
      QPB_RETURN_ERROR(stream, "array overflow");
   }
   {
      qpb_size_t size;
      qpb_istream_t substream;
      bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
      if (!result)
      {
         return false;
      }
      substream = qpb_istream_from_buffer(stream->buffer, size);
      result = decode_VisionCore_Road_FreespaceSector_Raw(&substream, &((VisionCore_Road_Freespace_Raw*)data)->sectors[((VisionCore_Road_Freespace_Raw*)data)->sectors_count++]);
      if (!result)
      {
         QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
      }
      stream->buffer += size;
   }
   return true;
}

static inline void encode_VisionCore_Road_Freespace_sectors(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceSector arrayData[], const qpb_size_t arraySize)
{
   if (arraySize > 0)
   {
      qpb_byte_t* sizePtr;
      qpb_byte_t* dataPtr;
      qpb_size_t size;
      qpb_byte_t dataToClear;
      for (qpb_size_t i = 0; i < arraySize; ++i)
      {
         *stream->buffer = 10;
         ++stream->buffer;
         sizePtr = stream->buffer;
         stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
         dataPtr = stream->buffer;
         encode_VisionCore_Road_FreespaceSector(stream, &arrayData[i]);
         /* Check size of encoded message and write size at the appropriate offset */
         size = (stream->buffer - dataPtr);
         stream->buffer = sizePtr;
         qpb_encode_varint(stream, (qpb_uint64_t)size);
         dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - sizePtr);
         /* Shift buffer back to end of varint */
         memmove(stream->buffer, dataPtr, size);
         stream->buffer += size;
         memset(stream->buffer, 0, dataToClear);
      }
   }
}

static bool decode_VisionCore_Road_Freespace_sectors(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   if ((((VisionCore_Road_Freespace*)data)->sectors_count + 1) > 60)
   {
      QPB_RETURN_ERROR(stream, "array overflow");
   }
   {
      qpb_size_t size;
      qpb_istream_t substream;
      bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
      if (!result)
      {
         return false;
      }
      substream = qpb_istream_from_buffer(stream->buffer, size);
      result = decode_VisionCore_Road_FreespaceSector(&substream, &((VisionCore_Road_Freespace*)data)->sectors[((VisionCore_Road_Freespace*)data)->sectors_count++]);
      if (!result)
      {
         QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
      }
      stream->buffer += size;
   }
   return true;
}

/* Encoding / decoding functions */
bool encode_VisionCore_Road_FreespaceSector_Raw(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceSector_Raw *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < VisionCore_Road_FreespaceSector_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_VisionCore_Road_FreespaceSector_Raw_boundaryPosition(stream, &data->boundaryPosition);
   encode_VisionCore_Road_FreespaceSector_Raw_type(stream, &data->type);
   encode_VisionCore_Road_FreespaceSector_Raw_confidence(stream, &data->confidence);
   return true;
}

bool decode_VisionCore_Road_FreespaceSector_Raw(qpb_istream_t *const stream, VisionCore_Road_FreespaceSector_Raw *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < VisionCore_Road_FreespaceSector_Raw_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = VisionCore_Road_FreespaceSector_Raw_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

bool encode_VisionCore_Road_FreespaceSector(qpb_ostream_t *const stream, const VisionCore_Road_FreespaceSector *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < VisionCore_Road_FreespaceSector_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_VisionCore_Road_FreespaceSector_boundaryPosition(stream, &data->boundaryPosition);
   encode_VisionCore_Road_FreespaceSector_Raw_type(stream, &data->type);
   encode_VisionCore_Road_FreespaceSector_Raw_confidence(stream, &data->confidence);
   return true;
}

bool decode_VisionCore_Road_FreespaceSector(qpb_istream_t *const stream, VisionCore_Road_FreespaceSector *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < VisionCore_Road_FreespaceSector_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = VisionCore_Road_FreespaceSector_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

bool encode_VisionCore_Road_Freespace_Raw(qpb_ostream_t *const stream, const VisionCore_Road_Freespace_Raw *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < VisionCore_Road_Freespace_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_VisionCore_Road_Freespace_Raw_sectors(stream, data->sectors, data->sectors_count);
   return true;
}

bool decode_VisionCore_Road_Freespace_Raw(qpb_istream_t *const stream, VisionCore_Road_Freespace_Raw *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < VisionCore_Road_Freespace_Raw_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = VisionCore_Road_Freespace_Raw_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

bool encode_VisionCore_Road_Freespace(qpb_ostream_t *const stream, const VisionCore_Road_Freespace *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < VisionCore_Road_Freespace_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_VisionCore_Road_Freespace_sectors(stream, data->sectors, data->sectors_count);
   return true;
}

bool decode_VisionCore_Road_Freespace(qpb_istream_t *const stream, VisionCore_Road_Freespace *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < VisionCore_Road_Freespace_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = VisionCore_Road_Freespace_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

