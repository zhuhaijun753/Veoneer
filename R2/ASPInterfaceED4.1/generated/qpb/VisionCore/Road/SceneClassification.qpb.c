/* Automatically generated qpb constant definitions */
/* Generated by Quick Protocol Buffers - 2 */

#include "VisionCore/Road/SceneClassification.qpb.h"
#include "Tools/QuickProtobuf/qpb_decode.h"
#include "Tools/QuickProtobuf/qpb_encode.h"

#if QPB_PROTO_HEADER_VERSION != 2
#error Regenerate this file with the current version of qpb generator.
#endif

static inline void encode_VisionCore_Road_RoadClassificationResult_Raw_isDetected(qpb_ostream_t *const stream, const bool *const data);
static bool decode_VisionCore_Road_RoadClassificationResult_Raw_isDetected(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_RoadClassificationResult_Raw_confidence(qpb_ostream_t *const stream, const uint8_t *const data);
static bool decode_VisionCore_Road_RoadClassificationResult_Raw_confidence(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_RoadCover_Raw_snow(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data);
static bool decode_VisionCore_Road_RoadCover_Raw_snow(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_RoadCover_Raw_gravel(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data);
static bool decode_VisionCore_Road_RoadCover_Raw_gravel(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_RoadCover_Raw_wet(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data);
static bool decode_VisionCore_Road_RoadCover_Raw_wet(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_SceneClassification_Raw_roadCover(qpb_ostream_t *const stream, const VisionCore_Road_RoadCover_Raw *const data);
static bool decode_VisionCore_Road_SceneClassification_Raw_roadCover(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_SceneClassification_Raw_tunnel(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data);
static bool decode_VisionCore_Road_SceneClassification_Raw_tunnel(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);
static inline void encode_VisionCore_Road_SceneClassification_Raw_fog(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data);
static bool decode_VisionCore_Road_SceneClassification_Raw_fog(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype);

#define VisionCore_Road_RoadClassificationResult_Raw_DECODERS_COUNT 3
const qpb_decoder_entry_t VisionCore_Road_RoadClassificationResult_Raw_decoders[VisionCore_Road_RoadClassificationResult_Raw_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_VisionCore_Road_RoadClassificationResult_Raw_isDetected },
   { &decode_VisionCore_Road_RoadClassificationResult_Raw_confidence }
};

#define VisionCore_Road_RoadCover_Raw_DECODERS_COUNT 4
const qpb_decoder_entry_t VisionCore_Road_RoadCover_Raw_decoders[VisionCore_Road_RoadCover_Raw_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_VisionCore_Road_RoadCover_Raw_snow },
   { &decode_VisionCore_Road_RoadCover_Raw_gravel },
   { &decode_VisionCore_Road_RoadCover_Raw_wet }
};

#define VisionCore_Road_SceneClassification_Raw_DECODERS_COUNT 4
const qpb_decoder_entry_t VisionCore_Road_SceneClassification_Raw_decoders[VisionCore_Road_SceneClassification_Raw_DECODERS_COUNT] = {
   { &qpb_skip_field } /* Id 0 is not valid and this is skipped one level up */,
   { &decode_VisionCore_Road_SceneClassification_Raw_roadCover },
   { &decode_VisionCore_Road_SceneClassification_Raw_tunnel },
   { &decode_VisionCore_Road_SceneClassification_Raw_fog }
};

static inline void encode_VisionCore_Road_RoadClassificationResult_Raw_isDetected(qpb_ostream_t *const stream, const bool *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 8;
      ++stream->buffer;
      qpb_encode_varint(stream, *data);
   }
}

static bool decode_VisionCore_Road_RoadClassificationResult_Raw_isDetected(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_varint(stream, (void*)&((VisionCore_Road_RoadClassificationResult_Raw*)data)->isDetected, sizeof(bool));
}

static inline void encode_VisionCore_Road_RoadClassificationResult_Raw_confidence(qpb_ostream_t *const stream, const uint8_t *const data)
{
   if (*data != 0)
   {
      *stream->buffer = 16;
      ++stream->buffer;
      qpb_encode_uvarint(stream, *data);
   }
}

static bool decode_VisionCore_Road_RoadClassificationResult_Raw_confidence(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   return qpb_decode_uvarint(stream, (void*)&((VisionCore_Road_RoadClassificationResult_Raw*)data)->confidence, sizeof(uint8_t));
}

static inline void encode_VisionCore_Road_RoadCover_Raw_snow(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 10;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_VisionCore_Road_RoadClassificationResult_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_VisionCore_Road_RoadCover_Raw_snow(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_VisionCore_Road_RoadClassificationResult_Raw(&substream, &((VisionCore_Road_RoadCover_Raw*)data)->snow);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_VisionCore_Road_RoadCover_Raw_gravel(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 18;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_VisionCore_Road_RoadClassificationResult_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_VisionCore_Road_RoadCover_Raw_gravel(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_VisionCore_Road_RoadClassificationResult_Raw(&substream, &((VisionCore_Road_RoadCover_Raw*)data)->gravel);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_VisionCore_Road_RoadCover_Raw_wet(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 26;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_VisionCore_Road_RoadClassificationResult_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_VisionCore_Road_RoadCover_Raw_wet(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_VisionCore_Road_RoadClassificationResult_Raw(&substream, &((VisionCore_Road_RoadCover_Raw*)data)->wet);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_VisionCore_Road_SceneClassification_Raw_roadCover(qpb_ostream_t *const stream, const VisionCore_Road_RoadCover_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 10;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_VisionCore_Road_RoadCover_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_VisionCore_Road_SceneClassification_Raw_roadCover(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_VisionCore_Road_RoadCover_Raw(&substream, &((VisionCore_Road_SceneClassification_Raw*)data)->roadCover);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_VisionCore_Road_SceneClassification_Raw_tunnel(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 18;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_VisionCore_Road_RoadClassificationResult_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_VisionCore_Road_SceneClassification_Raw_tunnel(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_VisionCore_Road_RoadClassificationResult_Raw(&substream, &((VisionCore_Road_SceneClassification_Raw*)data)->tunnel);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

static inline void encode_VisionCore_Road_SceneClassification_Raw_fog(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data)
{
   qpb_byte_t* start;
   qpb_byte_t* dataPtr;
   qpb_size_t size;
   qpb_byte_t dataToClear;
   *stream->buffer = 26;
   ++stream->buffer;
   start = stream->buffer;
   stream->buffer += QPB_VARINT_MAX_ENCODED_SIZE;
   dataPtr = stream->buffer;
   encode_VisionCore_Road_RoadClassificationResult_Raw(stream, data);
   if (stream->buffer > dataPtr)
   {
      /* Check size of encoded message and write size at the appropriate offset */
      size = (stream->buffer - dataPtr);
      stream->buffer = start;
      qpb_encode_varint(stream, (qpb_uint64_t)size);
      dataToClear = QPB_VARINT_MAX_ENCODED_SIZE - (stream->buffer - start);
      /* Shift buffer back to end of varint */
      memmove(stream->buffer, dataPtr, size);
      stream->buffer += size;
      memset(stream->buffer, 0, dataToClear);
   }
   else /* No data written shift buffer back */
   {
      stream->buffer = start - 1;
      memset(stream->buffer, 0, 1);
   }
}

static bool decode_VisionCore_Road_SceneClassification_Raw_fog(qpb_istream_t *const stream, void *const data, const qpb_wire_type_t wiretype)
{
   QPB_UNUSED(wiretype);
   qpb_size_t size;
   qpb_istream_t substream;
   bool result = qpb_decode_uvarint(stream, &size, sizeof(qpb_size_t));
   if (!result)
   {
      return false;
   }
   substream = qpb_istream_from_buffer(stream->buffer, size);
   result = decode_VisionCore_Road_RoadClassificationResult_Raw(&substream, &((VisionCore_Road_SceneClassification_Raw*)data)->fog);
   if (!result)
   {
      QPB_RETURN_ERROR(stream, QPB_GET_ERROR(&substream));
   }
   stream->buffer += size;
   return result;
}

/* Encoding / decoding functions */
bool encode_VisionCore_Road_RoadClassificationResult_Raw(qpb_ostream_t *const stream, const VisionCore_Road_RoadClassificationResult_Raw *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < VisionCore_Road_RoadClassificationResult_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_VisionCore_Road_RoadClassificationResult_Raw_isDetected(stream, &data->isDetected);
   encode_VisionCore_Road_RoadClassificationResult_Raw_confidence(stream, &data->confidence);
   return true;
}

bool decode_VisionCore_Road_RoadClassificationResult_Raw(qpb_istream_t *const stream, VisionCore_Road_RoadClassificationResult_Raw *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < VisionCore_Road_RoadClassificationResult_Raw_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = VisionCore_Road_RoadClassificationResult_Raw_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

bool encode_VisionCore_Road_RoadCover_Raw(qpb_ostream_t *const stream, const VisionCore_Road_RoadCover_Raw *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < VisionCore_Road_RoadCover_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_VisionCore_Road_RoadCover_Raw_snow(stream, &data->snow);
   encode_VisionCore_Road_RoadCover_Raw_gravel(stream, &data->gravel);
   encode_VisionCore_Road_RoadCover_Raw_wet(stream, &data->wet);
   return true;
}

bool decode_VisionCore_Road_RoadCover_Raw(qpb_istream_t *const stream, VisionCore_Road_RoadCover_Raw *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < VisionCore_Road_RoadCover_Raw_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = VisionCore_Road_RoadCover_Raw_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

bool encode_VisionCore_Road_SceneClassification_Raw(qpb_ostream_t *const stream, const VisionCore_Road_SceneClassification_Raw *const data)
{
   /* Optimize by checking that size is enough on top level? */
   if ((qpb_size_t)(stream->end - stream->buffer) < VisionCore_Road_SceneClassification_size)
   {
      QPB_RETURN_ERROR(stream, "Buffer to small");
   }
   encode_VisionCore_Road_SceneClassification_Raw_roadCover(stream, &data->roadCover);
   encode_VisionCore_Road_SceneClassification_Raw_tunnel(stream, &data->tunnel);
   encode_VisionCore_Road_SceneClassification_Raw_fog(stream, &data->fog);
   return true;
}

bool decode_VisionCore_Road_SceneClassification_Raw(qpb_istream_t *const stream, VisionCore_Road_SceneClassification_Raw *const data)
{
   qpb_uint64_t tag;
   qpb_uint64_t id;
   qpb_wire_type_t wireType;
   bool result = true;
   while (stream->end != stream->buffer)
   {
      result = qpb_decode_uvarint(stream, &tag, sizeof(qpb_uint64_t));
      if (!result)
      {
         return false;
      }
      /* Extended support for NULL termination */
      if (tag == 0)
      {
         return true;
      }
      id = qpb_id(tag);
      wireType = (qpb_wire_type_t)qpb_wireType(tag);
      if (id < VisionCore_Road_SceneClassification_Raw_DECODERS_COUNT)
      {
         qpb_decoder_t decoder = VisionCore_Road_SceneClassification_Raw_decoders[id].func;
         result = decoder(stream, data, wireType);
      }
      else
      {
         result = qpb_skip_field(stream, NULL, wireType);
      }
      if (!result)
      {
         return false;
      }
   }
   return result;
}

